基础语法与C++差异

```go
// 变量
var x int = 10
x := 10
```

```go
// 逻辑结构
if a>b {}
for i := 0; i<5; i++ {}
```

```go
// 函数
func main(){}
```


数据类型

```go
 // string 不可变

// 数组 大小属于类型 不可变
[5]int

// map 基于哈希实现

// struct 只包含数据，方法附加在结构体外

// 函数类型

// interface类型
```
